export class ingredient{
    // public name: string;
    // public amount: number;
    // constructor(name:string,amount:number){
    //     this.name = name;
    //     this.amount = amount;
    // }
    constructor(public name:string,public amount: number){}
    // These both work the same way, the one being used here is the shortcut method for constructor
}